# GLM Vision Bridge v1.1.3

## 修复

- 管理资源路由现在只返回固定 HTML/JS 模板，不再嵌入运行时配置、模型目录或事件日志，也不会在未认证请求中读取 CPA 配置或调用本地模型 API。
- 配置、模型目录和事件仅通过受 CPA Management Key 保护的 `/v0/management/...` 接口加载。
- 修复静态加载模式下事件渲染函数名错误及模型下拉框未重建的问题。

## 管理页面

- 移除插件页面中的管理密码输入框。
- 自动复用 CPA 管理中心已保存在同源存储中的登录凭据，兼容当前 `cli-proxy-auth` 加密状态和旧版 `managementKey` 存储。
- 页面没有可复用凭据时失败关闭，只提示从已登录的 CPA 管理中心进入，不开放未认证保存接口。

## 验证

- `go test ./...`
- `go vet ./...`
- `go test -race ./...`
- 资源路由静态边界、无密码框、凭据复用和版本一致性回归测试
- 73 张缓存历史图片与 40 张归档图片性能基准
